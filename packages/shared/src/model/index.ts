/**
 * Blocos de construção de domínio compartilhados.
 *
 * `Entity` é a entidade base — `id` (UUID v4), identidade por `id` e o
 * contrato `validate()` sobre o módulo de validação.
 */
export * from './entity'
